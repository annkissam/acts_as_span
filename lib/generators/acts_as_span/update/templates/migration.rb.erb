class <%= migration_class_name %> < ActiveRecord::Migration
  def self.up
    add_column :<%= name.underscore.camelize.tableize %>, :start_date, :date
    add_column :<%= name.underscore.camelize.tableize %>, :end_date, :date
  end

  def self.down
    remove_column :<%= name.underscore.camelize.tableize %>, :start_date
    remove_column :<%= name.underscore.camelize.tableize %>, :end_date
  end
end