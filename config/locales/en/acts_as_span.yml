en:
  acts_as_span:
    end_date_propagator:
      one: End Date Propagator
      other: End Date Propagators
      rollback: ${acts_as_span.end_date_propagator.one} did not propagate.
  activerecord:
    errors:
      messages:
        end_date_propagator:
          propagation_failure: "%{parent} could not propagate
           %{end_date_field_name} to %{child}:\n%{reason}"
        not_within_parent_date_span: Must exist within the %{parent} date span
